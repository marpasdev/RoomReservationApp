<Window x:Class="RoomReservationSystem.Admin.EditRoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Room" Height="500" Width="400"
        WindowStartupLocation="CenterOwner">
 <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Name"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Capacity"/>
        <TextBox Grid.Row="3" x:Name="CapacityBox" Margin="0,0,0,10"/>

        <Label Grid.Row="4" Content="Max Duration (minutes)"/>
        <TextBox Grid.Row="5" x:Name="MaxDurationBox" Margin="0,0,0,10"/>

        <Label Grid.Row="6" Content="Equipment"/>
        <DataGrid Grid.Row="7"
                  x:Name="EquipmentGrid"
                  AutoGenerateColumns="False"
                  CanUserSortColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Include" Binding="{Binding IsSelected}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click"/>
            <Button Content="Save" Click="Save_Click"/>
        </StackPanel>
    </Grid>   
</Window>
